import Foundation
import Testing

@testable import AppScreenshotKitCLI

final class BezelFetcherTests {
    @Test func testParseSketchPageJSON() throws {
        //        // Parse document json
        //        let documentPath = Bundle.module.path(forResource: "document", ofType: "json")!
        //        let jsonDecoder = JSONDecoder()
        //        let data = try Data(contentsOf: URL(fileURLWithPath: documentPath))
        //        let document = try jsonDecoder.decode(SketchPage.self, from: data)
        //        print(document.imageNameMap())
    }
}
